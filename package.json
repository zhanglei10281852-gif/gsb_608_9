{
  "name": "csvsql-mini",
  "version": "0.1.0",
  "description": "Mini CSV SQL CLI: run SQL queries against CSV files",
  "bin": {
    "csvsql-mini": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p .",
    "start": "node dist/cli.js"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/node": "^20.0.0"
  }
}
