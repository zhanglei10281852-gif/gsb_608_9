{
  "name": "csvsql",
  "version": "1.0.0",
  "description": "A mini command-line tool to run SQL queries on CSV files",
  "main": "dist/cli.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/cli.js"
  },
  "devDependencies": {
    "@types/node": "^25.9.2",
    "typescript": "^5.3.0"
  }
}
